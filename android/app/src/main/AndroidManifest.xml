<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <!--
        INTERNET is required by the WebView host even though the app makes no
        network requests of its own: every asset is bundled and privacy.test.ts
        fails the build if a fetch, XHR, WebSocket or sendBeacon appears in src.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        The voice games read microphone AMPLITUDE and nothing else. No
        MediaRecorder, no retained buffer, no upload, and never
        SpeechRecognition. Without this permission getUserMedia fails silently
        on device while passing every test in CI, so the games look broken for a
        reason that never reproduces on a desktop.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Declared optional so the Play Store does not hide the app from tablets
        and other devices with no microphone. Every voice game ships a
        no-microphone fallback, so the app is fully usable without one.
    -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
